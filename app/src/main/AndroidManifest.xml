<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ningxiang.passwordbox">

    <application
        android:name=".PasswordApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SecurityActivity">

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AuthActivity"
            android:configChanges="orientation|screenSize|density|fontScale|keyboard|navigation|locale|touchscreen"/>
        <activity
            android:name=".PasswordUiContainer"
            android:label="@string/title_activity_password_ui"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>